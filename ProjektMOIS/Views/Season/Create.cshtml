@model ProjektMOIS.Model.Season
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Sezóny - přidat";
}

<div class="container">
    <div class="row">
        <div class="col-md-3">
            <ul class="nav nav-pills nav-stacked">
                <li><a href="/Season">Přehled sezón</a></li>
                <li class="active"><a href="/Season/Create">Přidat</a></li>
            </ul>
        </div>
        <div class="col-md-9">
            <div class="alert alert-info">Přidejte do databáze novou sezónu.</div>
            @using (Html.BeginForm("Create", "Season", FormMethod.Post, new { role = "form", @class = "form-horizontal" }))
            { 
                <div class="form-group">
                    <label for="inputValue" class="col-sm-2 control-label">Ročník</label>
                    <div class="col-sm-6">
                        @Html.TextBoxFor(m => m.Value, new { @class = "form-control", id = "inputValue", placeholder = "Ročník" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-6">
                        <button type="submit" class="btn btn-primary">Přidat</button>
                    </div>
                </div>
                @Html.ValidationSummary()
            }
        </div>
    </div>
</div>


